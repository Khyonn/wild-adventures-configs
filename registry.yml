# server port
server:
  port: 80

# registry
eureka:
  client:
    registerWithEureka: false
    fetchRegistry: false
    serviceUrl:
      defaultZone: http://edgeregistry/eureka/
  instance:
    preferIpAddress: true
